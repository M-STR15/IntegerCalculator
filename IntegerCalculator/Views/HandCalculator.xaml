<UserControl
    x:Class="IntegerCalculator.Views.HandCalculator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:IntegerCalculator.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="clr-namespace:IntegerCalculator.UIComponents;assembly=IntegerCalculator.UIComponents"
    Width="400"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!--  Style pro položku historického výpočtu  -->
        <Style x:Key="HistoryItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="HorizontalContentAlignment" Value="Right" />
            <Setter Property="Foreground" Value="#77FF77" />
            <Setter Property="Background" Value="#001D00" />
            <Setter Property="BorderBrush" Value="#0A3A0A" />
            <Setter Property="BorderThickness" Value="2" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border
                            x:Name="Bd"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">

                            <ContentPresenter HorizontalAlignment="Right" />
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="8"
                                    ShadowDepth="0"
                                    Color="#00FF00" />
                            </Border.Effect>
                        </Border>

                        <ControlTemplate.Triggers>
                            <!--  Hover  -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#003300" />
                            </Trigger>

                            <!--  Vybraná položka  -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#005500" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White" />
        </Style>
    </UserControl.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Formula:" />
        <uc:Display
            Grid.Row="1"
            Margin="5"
            Value="{Binding ValueDisplay, Mode=TwoWay}" />
        <Label Grid.Row="2" Content="Procedure calculat:" />
        <ListBox
            x:Name="HistoryList"
            Grid.Row="3"
            Height="100"
            Margin="5"
            Background="#002900"
            BorderBrush="#0A3A0A"
            BorderThickness="3"
            FontFamily="/YourAssemblyName;component/Fonts/#DS-Digital"
            Foreground="#66FF66"
            ItemsSource="{Binding CalculationSteps}"
            ScrollViewer.VerticalScrollBarVisibility="Auto" />
        <uc:Keyboard
            Grid.Row="4"
            ClickCharacterCommand="{Binding InsertCharacterCommand}"
            ClickEqualsCommand="{Binding EqualsCommand}"
            ClickOnCCommand="{Binding ClearCommand}" />
    </Grid>
</UserControl>
